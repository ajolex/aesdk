### 2026-05-13 - Bug - Changelog Audit Hardening
- **Issue:** The changelog audit found that recent AESDK features were mostly implemented, but agent preflight and run status semantics had small guardrail gaps.
- **Resolution:** Hardened preflight so requested method context must match both the PAP strategy and proposal estimator, made `AnalysisRunResult.blocked` include sandbox execution blocks, and added regression coverage for Stata replay language preservation plus GCP/Azure KMS injected-client paths.
- **Implications:** Changed `src/aesdk/agent/preflight.py`, `src/aesdk/agent/run.py`, `src/aesdk/trace/kms_providers.py`, `tests/unit/agent/test_agent_api.py`, `tests/unit/trace/test_kms_signing.py`, and `tests/integration/test_replay_workflow.py`; full suite passed with 65 tests.
- **Difficulty:** Medium - the audit involved reconciling two subagent passes and separating real defects from already-modified working-tree changes.
- **Lessons:** For agent-facing research guardrails, preflight must check consistency across requested method, PAP strategy, and proposal estimator, and result flags must reflect both governance and execution blocks.
### 2026-05-13 - Feature - Python/Stata/R Guardrail Parity
- **Issue:** AESDK had Python-first execution and knowledge packaging while Stata and R needed to match the public package surface analysts expect in development economics workflows.
- **Resolution:** Added Stata and R execution dispatch behind preflight, R package allowlisting, replay language normalization, CLI sandbox diagnostics, official software source validation, and Python/R/Stata recipe parity for public packs where mature sources exist.
- **Implications:** Updated sandbox runner/whitelist, agent and CLI run paths, trace replay, knowledge packs, official software source metadata, docs, templates, and tests; nonlinear DiD remains explicitly R-only until mature Python/Stata recipes are registered.
- **Difficulty:** Hard - Required reconciling runtime behavior, audit-trace semantics, package data, econometric recipe coverage, and documented exceptions across multiple public surfaces.
- **Lessons:** When adding a language bridge, audit execution cwd, runtime diagnostics, replay metadata, import/package allowlists, official recipe sources, package-data inclusion, and per-pack parity tests together.
### 2026-05-13 - Bug - Panel Cluster Declaration Guardrails
- **Issue:** Panel proposals could claim clustered or two-way-clustered inference without declaring the cluster level or both cluster dimensions.
- **Resolution:** Added Wooldridge panel rules blocking missing cluster levels and one-dimensional two-way clustering, plus validator helpers for parsing scalar and list-valued cluster declarations.
- **Implications:** Changed `src/aesdk/protocol/validator.py`, `src/aesdk/governance/rules/wooldridge_panel.rules.yaml`, and `tests/unit/protocol/test_validator_rules.py`; full suite passed with 78 tests.
- **Difficulty:** Medium - The fix required extending the safe rule-expression evaluator while preserving existing panel and DiD validation behavior.
- **Lessons:** Inference guardrails must force cluster levels and multiway cluster dimensions into the PAP/proposal record rather than treating the word "clustered" as sufficient.
### 2026-05-14 - Feature - Curriculum Rules and Exhaustive Executable Coverage
- **Issue:** AESDK's governance and knowledge organization still mixed author-named rule files with method packs, and newer econometric packs were guidance-heavy rather than executable.
- **Resolution:** Reorganized rule files by econometric topic/method, added a curriculum map, enforced online citation locators, added method-topic organization metadata to every pack, expanded validator context for method-specific PAP/proposal fields, and promoted all current pack guidance into 72 executable rules across OLS/CEF, IV/2SLS, panel inference, DiD, RDD, matching, synthetic control, nonlinear DiD, GMM, limited dependent variables, time series, and citation integrity.
- **Implications:** Changed governance rules, `src/aesdk/protocol/validator.py`, knowledge catalog/source metadata/packs, CLI `rules list`, docs, and protocol/catalog tests; subagent audit findings were fixed for silent eval failures, DiD/Synth aliases, missing panel/IV/GMM rules, and malformed DOI-like text.
- **Difficulty:** Hard - Required turning prose guardrails into safe-evaluable conditions while keeping author/source citations as evidence rather than SDK organization.
- **Lessons:** AESDK authority should be organized by econometric method/topic and curriculum stage, with sources as verifiable evidence; every knowledge pack should have matching executable rule coverage and tests preventing author-named governance regressions.
### 2026-05-14 - Feature - Experimental RCT Guardrails
- **Issue:** AESDK lacked first-class experimental-method/RCT support for ATE, ITT, ATT/ToT, LATE, randomization design, compliance, attrition, and spillover governance.
- **Resolution:** Added `experimental_rct` to method aliases, curriculum, method protocols, source map, PAP schema, source registry, and knowledge packs; registered J-PAL Randomization, J-PAL Data Analysis, and the local Bédécarrats/Guérin/Roubaud RCT critique; added 15 executable `RCT-*` rules for assignment, estimand, LATE assumptions, compliance, clustered inference, balance, attrition, spillovers, power, and trial/PAP registration.
- **Implications:** Updated `src/aesdk/agent/preflight.py`, `src/aesdk/protocol/validator.py`, `src/aesdk/governance/schemas/pap_schema.yaml`, `src/aesdk/governance/rules/experimental_rct.rules.yaml`, `src/aesdk/knowledge/packs/experimental_rct.yaml`, source metadata, docs, and tests; full suite passed with 106 tests and 87 executable rules.
- **Difficulty:** Medium - The work required preserving RCT benefits while adding guardrails against overclaiming, noncompliance confusion, and assignment/inference mismatches.
- **Lessons:** Experimental methods must distinguish randomized assignment from treatment receipt, force an estimand declaration before code, and treat attrition, spillovers, compliance, and clustered assignment as core governance fields rather than optional prose.
### 2026-05-14 - Bug - RCT Audit Follow-Up Hardening
- **Issue:** Subagent audit found that first-pass RCT support was too easy to bypass for ToT, ordinary RCT PAPs declaring LATE, imperfect-compliance ATE, clustered RCT inference levels, stratification, attrition, and spillover/SUTVA details.
- **Resolution:** Added RCT PAP drafting, ToT aliases/schema support, validator context fields, placeholder-text detection, compliance-type normalization, a broader RCT unit hierarchy, stricter RCT rules through `RCT-022`, source-inventory alignment, and regression tests for the audited failure modes.
- **Implications:** Changed `src/aesdk/agent/pap_draft.py`, `src/aesdk/agent/preflight.py`, `src/aesdk/protocol/validator.py`, `src/aesdk/governance/schemas/pap_schema.yaml`, `src/aesdk/governance/rules/experimental_rct.rules.yaml`, RCT knowledge/source metadata, docs, and tests; full suite passed with 120 tests and 94 executable rules.
- **Difficulty:** Medium - The hard part was making the RCT checks fire from the PAP's declared estimand even when the proposal uses the generic `RCT` estimator.
- **Lessons:** RCT governance must bind rules to the declared estimand inside `rct_block`, not only to the proposal estimator label; cluster-randomized designs need both an inference type and an explicit cluster level.
### 2026-05-14 - Bug - R/Stata Recipe and Sandbox Parity
- **Issue:** Public method packs had grown to include Python/R/Stata recipes across the full econometrics surface, but the execution guard and agent templates had drifted behind the current package surface.
- **Resolution:** Added missing Python recipe packages (`pyfixest`, `rdrobust`, `scpi`) to the sandbox allowlist, added a regression test that every bundled Python/R recipe package is allowed by the sandbox, and updated bundled `AGENTS.md`/`CLAUDE.md` templates to include `experimental_rct` plus governed Python, Stata, and R execution guidance.
- **Implications:** Changed `src/aesdk/sandbox/whitelist.yaml`, `src/aesdk/agent/templates/AGENTS.md`, `src/aesdk/agent/templates/CLAUDE.md`, `tests/unit/knowledge/test_catalog.py`, `tests/unit/agent/test_agent_api.py`, `README.md`, and `docs/PROJECT_FUNCTIONALITY.md`; full suite passed with 121 tests and 94 executable rules.
- **Difficulty:** Medium - The key risk was not econometric logic but package-surface drift between knowledge recipes, sandbox allowlists, and agent-facing instructions.
- **Lessons:** Whenever adding or changing method-pack code recipes, verify Python/R sandbox allowlists and bundled agent templates in the same pass so AESDK never recommends code that governed execution rejects.
